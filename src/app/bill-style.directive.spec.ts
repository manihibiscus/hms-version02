import { BillStyleDirective } from './bill-style.directive';

describe('BillStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BillStyleDirective();
    expect(directive).toBeTruthy();
  });
});
