import { BmiCalcularPipe } from './bmi-calcular.pipe';

describe('BmiCalcularPipe', () => {
  it('create an instance', () => {
    const pipe = new BmiCalcularPipe();
    expect(pipe).toBeTruthy();
  });
});
